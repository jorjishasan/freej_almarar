[phases.setup]
nixPkgs = ["nodejs", "pnpm"]

[phases.install]
cmds = ["pnpm install"]

[phases.build]
cmds = ["pnpm --filter backend build"]

[start]
cmd = "pnpm --filter backend start"

[variables]
NPM_CONFIG_PRODUCTION = "false"
